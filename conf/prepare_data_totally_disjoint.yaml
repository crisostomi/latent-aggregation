defaults:
  - dataset: cifar100


image_key: img
label_key: y
num_tasks: 2
classes_partitions:
  - [0, 50]   # C1   [0, 50] for cifar, [0, 100] for tiny_imagenet
  - [50, 100] # C2   [50, 100] for cifar, [100, 200] for tiny_imagenet
val_perc_per_task: 0.1
num_anchors: 1000

seed: 42
use_cached: true

dataset_folder: ${oc.env:PROJECT_ROOT}/data/${dataset.name}
output_path: "${dataset_folder}/totally_disjoint/partition-1"
